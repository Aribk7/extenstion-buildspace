document.addEventListener('DOMContentLoaded', function() {
  // replace dom with buildspace.so
  
});


// chrome.tabs.onCreated.addListener(function (tab) {
//     chrome.tabs.update(tab.id, {
//         url: "https://tooltips.ai"
//     });